Repo used only for releases. At the moment, It should only include installers for medicare and acadmey apps. 

1 file per platform, These should be self contained and apps should allow self-update check and restart

`naaply/` holds the Tailor Shop app's public pages: `privacy.html` (the Play
listing's privacy policy, served by GitHub Pages) and `version.json` (the
in-app update feed — bump `version_code`/`version_name` on each release).
