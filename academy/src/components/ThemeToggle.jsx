// ThemeToggle is intentionally a no-op placeholder.
// The application currently uses light theme only (enforced by ThemeContext).
// This component can be expanded in a future release if theme switching is needed.
export const ThemeToggle = () => null;
