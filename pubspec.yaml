name: academia
description: Academy - Secure Multi-Role Educational Management Platform (Dart Backend & React Frontend)
version: 0.7.0

environment:
  sdk: ^3.12.2

# Add regular dependencies here.
dependencies:
  sqlite3: ^2.4.0
  crypto: ^3.0.3
  bcrypt: ^1.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
